[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cellyoulite"
dynamic = ["version"]
description = "Organoid segmentation and time-series analysis for wet-lab biologists"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
    "numpy>=1.24",
    "scikit-image>=0.22",
    "imagecodecs>=2024.1.1",   # decode LZW/compressed TIFFs via tifffile
    "opencv-python-headless>=4.9",
    "pandas>=2.0",
    "matplotlib>=3.8",
    "plotly>=5.18",
]

[project.optional-dependencies]
dev = ["pytest>=7", "ruff>=0.3"]
# Heavy deps for the standalone analysis tool (cellyoulite-analyze). Kept out
# of the lean web-server install. Cellpose-SAM ("cpsam") needs cellpose>=4.
analyze = ["cellpose>=4.0", "rich>=13.0"]

[project.scripts]
cellyoulite = "cellyoulite.launcher:main"
cellyoulite-analyze = "cellyoulite.analyze_cli:main"

[tool.hatch.version]
path = "cellyoulite/__version__.py"

[tool.hatch.build.targets.wheel]
packages = ["cellyoulite"]

[tool.hatch.build.targets.wheel.force-include]
"cellyoulite/web/templates" = "cellyoulite/web/templates"
"cellyoulite/web/static" = "cellyoulite/web/static"
