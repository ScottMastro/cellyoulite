__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.DS_Store

# Raw experimental data — kept locally, not in git
/data/

# Local alignment QC outputs
/align_check/

# Per-method circle-detection QC stills (regenerable from data/)
/circle_check/

# On-disk alignment cache (regenerated on demand from data/)
/.align_cache/

# Cellpose per-frame detection cache (regenerated by cellpose_batch.py)
/.cellpose_cache/

# Rendered/resized image cache (regenerated on demand by /api/image)
/.img_cache/

# Rendered cellpose-edges overlay cache (regenerated on demand by /api/cellpose-edges)
/.edges_cache/

# Per-well linked tracks (regenerated by cellpose_track.py from the cellpose cache)
/tracks/

# Experiment output (annotated stills, HTML reports, etc.)
/out/

# Password-less user-account registry (runtime state, created on first use)
/users.json

# SQLite database (+ WAL/SHM sidecars) — created/migrated at runtime
/cellyoulite.db
/cellyoulite.db-wal
/cellyoulite.db-shm

# Editor / agent state
/.claude/
